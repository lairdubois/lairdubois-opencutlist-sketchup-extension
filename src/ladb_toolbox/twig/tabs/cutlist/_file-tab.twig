{% extends 'tabs/base-file-tab.twig' %}

{% block inner %}{{ parent() }}{% if pageLabel %} - <em>{{ pageLabel }}</em>{% endif %}<time>{{ generateAt|date(('default.date_format'|i18next())) }}</time>{% endblock %}