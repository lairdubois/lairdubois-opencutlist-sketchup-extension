<nav id="ladb_leftbar" class="ladb-leftbar no-print">
    <a id="ladb_btn_minimize" href="#" style="display: none;"><i class="glyphicon glyphicon-chevron-left"></i><div>Réduire</div></a>
    <a id="ladb_btn_maximize" href="#"><i class="glyphicon glyphicon-chevron-right"></i><div>Agrandir</div></a>
    {% for tabDef in tabDefs %}
        <a id="ladb_btn_{{ tabDef.name }}" href="#"><i class="{{ tabDef.icon }}"></i><div>{{ tabDef.label }}</div></a>
    {% endfor %}
</nav>
<div id="ladb_wrapper" style="display: none;">
    {% if not htmlDialogCompatible and not compatibilityAlertHidden %}
        <div id="ladb_compatibility_alert" class="no-print alert alert-danger" style="margin-bottom: 0; font-size: 90%;">
            <div class="media">
                <button id="ladb_btn_close_compatibility_alert" type="button" class="close" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <div class="media-left">
                    <i class="glyphicon glyphicon-exclamation-sign" style="font-size: 150%;"></i>
                </div>
                <div class="media-body">
                    <h4 class="media-heading">Attention</h4>
                    <p>Vous utilisez une ancienne version de Sketchup ({{ sketchupVersion }}). Ce plugin fonctionne, mais révèle certains bugs dus à cette ancienneté.<br/>Pour profiter pleinement de ce plugin, il est préconisé d'utiliser <strong>Sketchup 2017</strong>.</p>
                </div>
            </div>
        </div>
    {% endif %}
    <div id="ladb_bottombar" class="ladb-bottombar no-print">
        <ul>
            <li><a href="https://www.lairdubois.fr/blog/56-un-autre-plugin-sketchup-pour-les-fiches-de-debit.html" target="_blank">SU{{ sketchupVersion }} - {{ currentOS }} - {{ version }}</a></li>
        </ul>
    </div>
</div>