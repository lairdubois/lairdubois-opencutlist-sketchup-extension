{# set errors #}

{% if errors and errors|length > 0 %}
    <div class="no-print alert alert-danger ladb-alert-errors">
        <ul>
        {% for error in errors %}
            <li>{{ error }}</li>
        {% endfor %}
        </ul>
    </div>
{% endif %}