{# set generateAt #}

{% extends 'tabs/base-file-tab.twig' %}

{% block inner %}{{ parent() }}{% if pageLabel %} - <em>{{ pageLabel }}</em>{% endif %}<span class="ladb-extra ladb-horizontal-left-separator">{{ generateAt|date(('default.date_format'|i18next())) }}</span>{% include 'core/_length-unit-extra.twig' %}{% endblock %}